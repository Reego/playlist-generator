function loadSongs(fileContent) {
    const lines = fileContent.split("\n");
    const songs = [];

    // for(let line of lines) {
    //     songs.push();
    // }
    return songs;
}

export {
    loadSongs,
};